# ERRORS.md — 判例式负向记忆（红线禁区，绝不重犯）

- 任何声称"已完成"的交付物，必须先用工具实际打开/校验过（如用 python-pptx 回读生成的 pptx
  确认文字无乱码、图形无重叠、图片无拉伸变形），不能凭生成过程顺利就断言完成。
- 不得把"检索得到的网络二手数据"直接当成项目一手数据写进 `PROJECT_FACTS.md` 的"核心技术体系"
  区块，必须放入"来源：网络检索"标注区，且与仓库原始 PDF 数据物理隔离展示。
- 沙箱环境**没有 LibreOffice/soffice**，且 `apt-get install` 无法访问 Debian 源（网络仅对
  `git`/`web_search`/`fetch_page`/`image_search` 等受控通道开放，`curl`/`apt` 直连会失败）。
  因此无法把 pptx 渲染成图片做像素级视觉验收。**替代方案**：用 python-pptx 读回所有图形的
  left/top/width/height，配合 matplotlib 画线框图 + PIL 把真实图片按坐标粘贴合成，做"结构性
  验收"（有没有重叠、有没有超出画布、图片长宽比对不对），不要假装能看到真实渲染效果。
- `generate_image` 工具**每个 Session 有 10 张的生成上限**，用完后再调用会直接报错。做多图标
  批量生成前，先规划好总数（例如"7 个核心场景图标 + 3 个问题图标"共 10 张打满），
  剩余需求要用 python-pptx 原生形状（圆形编号、箭头等）替代，而不是继续尝试生成图片。
- 输出大体积制品（如另存一份完整备份 pptx）前，先用 `du -sh` 核算体积对总仓库大小的影响；
  本次曾生成一份 59MB 的"原第四页备份"文件（因 python-pptx 删除多余 slide 不会自动清理未引用
  的 media 部件，导致体积几乎不变），发现后立即删除，改为直接在文档中指向原文件的第 4 张幻灯片。

- 上传的图片附件（`/home/user/uploads/...`）在某些轮次里**无法通过文件系统读取**
  （`read_file` 报错，`uploads/` 目录甚至不存在），只有当图片作为消息正文内联展示时我才能
  看到。遇到"发了文件但工具读不到"的情况，第一时间如实告知用户，让对方把图片重新贴一次
  （内联粘贴而不是仅作为附件上传），不要假装看到了内容就编一份分析出来。

- 前轮（一轮方案/二轮任务书）遗留的两处口径错误，接手时不得再引用：
  ①"国际化发展潜力"不是 2026 新增评审点（2025 已有）；②`02-方案与评审文档/国创赛PPT修改方案.md` 的页码 P2–P21 相对 0827.pdf
  整体偏移 1 页（方案按 0823 的 21 页结构写），引用该文档必须按内容映射到物理页。
- PDF 目检不能只靠文本层：pypdf extract_text 会提取**画布外**的残留文字（如 0827 P1"参赛类别：新工科类"在 y=553，
  页面高 540），mymupdf search_for 又漏掉一部分文本对象。正确姿势：pypdf 全量文本 + PyMuPDF 渲染 PNG 逐页目检，
  两者对照才能发现"画面没有、文本层有"的残留（网评材料会被文本检索，这类残留必须清理）。

- 【装备聚焦改造·2026-08-29】改图签必须同步改图：曾只把 P3 图签"PEEK头骨植入物"改为"卫星太阳翼铰链"
  而没给换图方案（头骨照片会原样留在页面上图文穿帮），且新图签与四宫格第 1 格"航天器铰链"实质重复。
  **规则：任何一格修改必须整排四格拉通审，先渲染目检原图/确认替代图真实存在（ls + 渲染 P15/P16 验证）再落笔。**
- 【术语科学性红线·2026-08-29】严禁机械替换"材料→构件"：熔点/导热系数/结晶度/拉伸模量等本征量、
  转移膜/填料协同等机理表述、竞品材料牌号（Victrex 450FC30）**不可**换成构件主语（"构件熔点"是笑柄）；
  构件级实测（摩擦系数/磨损率/尺寸误差/批次波动）、工艺归因表述、装机交付语境**可以**换。
  合法工具箱只有三个：换主语（合规语境）/换指标（整卡换装备级实测值）/保留降权（PEEK 学名移到定语位）。
- 【素材引用三查·2026-08-29】引用素材路径前必须 ls 验证（曾把第二页配图的图写成第三页配图）；
  引用《毕业答辩》页码必须先提取文本核对锚点；AI 生成图/带水印图/竞品官方图（EOSTATE）进正稿前必须识别并标注。

- 【图签—图片绑定必须逐格核对·2026-08-29】`29 第一版.pdf` P3 四宫格 4 格中 3 格错配：①"航天用锁钩机构"配卫星渲染图；②"卫星太阳翼铰链机构"配的其实是 P15 左图的**锁钩**照片；④"核能用径向节流器"后半张是 08277 遗留的车间照。
  **规则**：改图签=改图。任何"换标签不换图"的操作，改完后必须把该区域裁出来与源页并排目检，否则宁可不改。
- 【改标签不改数字＝造假级风险·2026-08-29】P12 右下四卡数字未动而量纲被换（75% 摩擦系数→"成形翘曲与热离散"；40% 拉伸强度→"成形致密"）；P6 "±0.5%"与 P25 "±1%以内"同篇打架；P10 "温度场空间分辨率20μm"实为答辩的**仿真网格分辨率**（D22/D23 选参）而非测温能力。**规则：数字换标签必须回源页核对，且同篇同量必须 grep 一致性；仿真值/实测值/单件值/整机值四类数字不得互串。**
- 【审计脚本能力边界·2026-08-29】一号方案"227 条原文零失配"只验证文本相等，验证不了语义正确与图文绑定。**任何"零失配/全绿"结论都不等于可交付**，交付前必须加两道：图签↔图片绑定目检、数字↔量纲绑定回源。
- 【裁图边界会造假的"被裁"结论·2026-08-29】给 P14 裁 (480,140)-(900,300) 见"文字被裁"，实为**裁剪框切在文本中间**（文本 x1=943<页宽 960）；同理我一度把 P25/P26 也判成出框。**规则：判"出框/被裁"必须先 get_text('blocks') 比 bbox 与页宽、卡框坐标，裁图只用于确认不用于判定越界。**
- 【越画布图要分两类再说·2026-08-29】P4(-451,-51) 确实影响显示；但 P5 的 9 张、P11 的 1 张整张躲在页外是设计师留在画外的素材副本（不影响渲染，只脏源文件）。**规则：①部分越界且页面内有切边后果＝必修；②整张在页外＝清理源文件级问题，两者不可混成一条。**

## 2026-09-01 P2 素材策划 Session
- 本轮早期误将方案要求执行成了P2 PDF/脚本的临时操作；已在同一轮全部删除，工作区未留下PPT或PDF改动。以后接手本仓库先遵守“只提供方案、由人类排版”的角色边界。
